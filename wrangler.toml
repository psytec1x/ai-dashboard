# wrangler.toml — Cloudflare Pages & Workers configuration
# For Pages projects, most config is done via Cloudflare dashboard.
# This file is optional for Pages; used for Workers/Workers AI if needed.

name = "ai-dashboard"
compatibility_date = "2025-01-01"

# Pages build config is in the dashboard or via CLI:
#   wrangler pages dev dist/ --local
#   wrangler pages deploy --project-name ai-dashboard

[vars]
  # Add env vars here; override via Cloudflare dashboard → Settings → Environment Variables
